/*
In this Json file we have the data that we send to the API to get an entries
*/

{
  "url": "{{base_url}}",
  "headers": {
    "X-Api-Key": "{{api_key}}",
    "Content-Type": "application/json"
  },
  "endpoint": "/workspaces/{{id_workspace}}/user/{{id_user}}/time-entries"
}